---
phase: 01-mcp-tool-real-email-delivery
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - internal/mcpserver/server.go
  - internal/mcpserver/tool.go
  - internal/mcpserver/tool_test.go
  - cmd/server/main.go
autonomous: false
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, SEC-02, EMAIL-04]
must_haves:
  truths:
    - "`go run ./cmd/server` serves the MCP Streamable HTTP endpoint at /mcp and the initialize handshake echoes the client protocol version"
    - "An MCP client lists exactly one send_notification tool exposing subject/body/status and NO recipient field"
    - "A successful call returns a confirmation with recipient + message-id + timestamp (D-08)"
    - "A forced SMTP failure returns a clear tool error (IsError), not a silent success (D-09)"
    - "A real email lands in michael@blacktoaster.com's inbox (not spam), From the authenticated account, subject carrying the [Status] prefix"
  artifacts:
    - path: "cmd/server/main.go"
      provides: "Composition root: config→service→server→StreamableHTTPHandler on /mcp"
      contains: "NewStreamableHTTPHandler"
    - path: "internal/mcpserver/tool.go"
      provides: "SendInput/SendOutput + handler delegating to NotificationService"
      contains: "SendInput"
    - path: "internal/mcpserver/server.go"
      provides: "NewServer + AddTool(send_notification)"
      contains: "AddTool"
  key_links:
    - from: "cmd/server/main.go"
      to: "/mcp"
      via: "mux.Handle(\"/mcp\", NewStreamableHTTPHandler(...))"
      pattern: "/mcp"
    - from: "internal/mcpserver/tool.go"
      to: "NotificationService.Notify"
      via: "handler delegates render+send to the service"
      pattern: "Notify"
---

<objective>
Wire the walking skeleton together and prove it end-to-end: register the single `send_notification` tool (subject + markdown body + optional status enum, NO recipient — SEC-02/NOTIF-03), build the composition root in `cmd/server/main.go` (config → renderer + channel + service → `*mcp.Server` → `StreamableHTTPHandler` on `/mcp`), and run the deliverability + protocol-handshake gate so a real, correctly-formatted email lands in the inbox.

Purpose: This is the wire-last step of interface-first ordering and the moment the skeleton becomes a real, demonstrable capability. Covers NOTIF-01..05, EMAIL-04, SEC-02 (tool-schema half), and decisions D-08/D-09.
Output: A runnable server, handler unit tests, and a verified real-send.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md
@.planning/phases/01-mcp-tool-real-email-delivery/01-CONTEXT.md
@.planning/phases/01-mcp-tool-real-email-delivery/01-VALIDATION.md
@.env.example

<interfaces>
From internal/notify/service.go (Plan 01-01):
- type NotificationService struct { R Renderer; C Channel; Recipient string }
- func (s *NotificationService) Notify(ctx, subject, body, status string) (messageID string, ts time.Time, err error)
From internal/notify (Plans 01-02, 01-03):
- newRenderer() Renderer
- newEmailChannel(cfg config.Config) Channel
From internal/config (Plan 01-01):
- func Load() (Config, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tool registration + handler (success/error contract)</name>
  <files>internal/mcpserver/server.go, internal/mcpserver/tool.go, internal/mcpserver/tool_test.go</files>
  <read_first>
    - internal/notify/service.go (NotificationService.Notify signature from Plan 01-01)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md §"Code Examples §1" (tool input/output structs + registration) and §"Code Examples §4" (handler success result + auto error-wrapping), §"Common Pitfall 4" (SDK owns protocol negotiation)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-CONTEXT.md (D-08 success details, D-09 error contract, NOTIF-03 description guidance)
  </read_first>
  <behavior>
    - TestHandler_SuccessResult: with a mock Channel returning a canned message-id, the handler result message contains the recipient, the message-id, and an RFC3339 timestamp (D-08)
    - TestHandler_ErrorWraps: with a mock Channel returning an error, the handler returns a plain error (the SDK wraps it as CallToolResult IsError=true) — no silent success (D-09/NOTIF-05)
    - The SendInput struct exposes subject, body, optional status — and has NO recipient field (SEC-02)
  </behavior>
  <action>
    Create `internal/mcpserver/tool.go` with `SendInput` (fields: `Subject string`, `Body string`, `Status string` with a `json:"status,omitempty"` tag and jsonschema description per RESEARCH §1; status enum completed|waiting|info|error, default info handled in code per D-06 — NO recipient field, SEC-02) and `SendOutput{Message string}`. Implement `sendHandler(svc *notify.NotificationService) mcp.ToolHandlerFor[SendInput, SendOutput]` that calls `svc.Notify(ctx, in.Subject, in.Body, in.Status)`; on error return a plain wrapped error `fmt.Errorf("email send failed: %w", err)` (SDK auto-wraps to IsError, D-09 — no retry hint); on success return a `SendOutput{Message: ...}` reading "Email delivered to <recipient> — message-id <id>, <RFC3339 ts>" (D-08). Create `internal/mcpserver/server.go` with `NewServer(svc) *mcp.Server` using `mcp.NewServer` + `mcp.AddTool` registering `send_notification` with a Description (Claude's discretion, NOTIF-03) that states it emails Michael for task-done/blocked/waiting moments and that the recipient is FIXED so the agent never passes one. If the inline `jsonschema:"enum=...,default=..."` tag form does not take at build time (RESEARCH assumption A1), validate/default status in the handler instead — D-06 is trivial in Go either way. Write `internal/mcpserver/tool_test.go` with a `fakeChannel` mock (canned msg-id / forced error) + the real Renderer to cover the two behaviors above.
  </action>
  <verify>
    <automated>go test ./internal/mcpserver -run 'TestHandler'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/mcpserver -run TestHandler_SuccessResult` exits 0
    - `go test ./internal/mcpserver -run TestHandler_ErrorWraps` exits 0
    - Source assertion: `SendInput` declares no recipient/`to` field (SEC-02)
    - Source assertion: exactly one `mcp.AddTool(..., "send_notification" ...)` registration (NOTIF-01)
    - The tool Description states the recipient is fixed (NOTIF-03)
  </acceptance_criteria>
  <done>The tool is registered with the correct schema; handler returns D-08 proof-of-delivery on success and a wrapped error on failure; both tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Composition root — serve /mcp over Streamable HTTP</name>
  <files>cmd/server/main.go</files>
  <read_first>
    - internal/mcpserver/server.go (NewServer from Task 1)
    - internal/notify/service.go, internal/notify/render.go, internal/notify/email.go (constructors)
    - internal/config/config.go (Load)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md §"Code Examples §2" (Streamable HTTP wiring) and §"Common Pitfall 6" (localhost protection)
  </read_first>
  <action>
    Create `cmd/server/main.go`: load config via `config.Load()` (fatal on error); construct `notify.NotificationService{R: newRenderer(), C: newEmailChannel(cfg), Recipient: cfg.NotifyRecipient}`; build the server with `mcpserver.NewServer(svc)`; wrap it with `mcp.NewStreamableHTTPHandler(func(*http.Request) *mcp.Server { return srv }, nil)` (the SDK handles both POST and the streaming GET, and owns protocol-version negotiation per Pitfall 4 — do NOT hand-roll version logic); register it on a `http.NewServeMux()` at `mux.Handle("/mcp", handler)`; `http.ListenAndServe(":"+cfg.Port, mux)`. Leave room for Phase 2 to add `/healthz`, `/readyz`, and a bearer-auth wrapper around `/mcp` — do NOT add them now. Connect via `http://localhost:8080/mcp` (loopback satisfies the SDK's DNS-rebinding protection). Optionally pass `&mcp.StreamableHTTPOptions{JSONResponse: true}` to ease local curl debugging.
  </action>
  <verify>
    <automated>go build ./cmd/server && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/server` exits 0
    - Source assertion: `mux.Handle("/mcp", ...)` with a `NewStreamableHTTPHandler` (NOTIF-04)
    - Source assertion: the service is wired with `newRenderer()` and `newEmailChannel(cfg)` and `cfg.NotifyRecipient`
    - No `/healthz`/`/readyz`/bearer middleware present (those are Phase 2)
  </acceptance_criteria>
  <done>`go run ./cmd/server` starts and serves the MCP endpoint on /mcp; full suite `go test ./...` green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deliverability + protocol-handshake gate</name>
  <files>(verification only — no source files modified)</files>
  <what-built>A runnable MCP server (`go run ./cmd/server`) serving `send_notification` over Streamable HTTP at /mcp, wired to real markdown rendering and real Gmail SMTP delivery to michael@blacktoaster.com.</what-built>
  <read_first>
    - .planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md §"Deliverability Spike" (Show-original checklist) and §"Local Dev" (curl handshake)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
    Human-run verification gate (no code changes). Start the server and exercise the live path end-to-end: confirm the MCP protocol handshake echoes a supported version, send a real email, and verify it lands in the inbox (not spam) via Gmail "Show original". Follow the numbered steps in how-to-verify exactly.
  </action>
  <how-to-verify>
    1. Copy `.env.example` to `.env` and fill `GMAIL_APP_PASSWORD` (16-char Gmail app password — a normal password will fail auth). Start the server: `go run ./cmd/server`.
    2. Protocol handshake (NOTIF-04): run the curl `initialize` POST from RESEARCH §"Local Dev" against `http://localhost:8080/mcp` with `Accept: application/json, text/event-stream`. Confirm the response echoes the client `protocolVersion` (e.g. "2025-06-18") or returns a supported version (e.g. "2025-11-25").
    3. Real send (EMAIL-01/EMAIL-04): run `go test -tags=integration ./internal/notify -run TestRealSend` OR call the tool via MCP Inspector. Confirm a non-empty message-id is returned.
    4. Inbox check (the Phase-1 gate): in Gmail open the message → "Show original". Confirm SPF: PASS, DMARC: PASS, the message is in Inbox NOT Spam, `From:` = michael@blacktoaster.com, and the subject carries the `[Status]` prefix (e.g. `[Info] ...`).
    5. Send one `error` and one `completed` status to eyeball banner colors (red / green) render in Gmail web + mobile (D-03).
    Note: enabling Workspace DKIM for blacktoaster.com is a recommended hardening (owner action, not code) but is NOT a blocker for this gate.
  </how-to-verify>
  <verify>
    <human-check>Initialize handshake echoes a supported protocolVersion; a real email lands in the Inbox (not Spam) with SPF/DMARC PASS, correct From, and the [Status] subject prefix; a forced failure surfaces as a clear tool error.</human-check>
  </verify>
  <acceptance_criteria>
    - The initialize handshake response echoes a supported protocolVersion (NOTIF-04)
    - A real email arrives in the Inbox (not Spam) with SPF/DMARC PASS, correct From, and the [Status] subject prefix (EMAIL-01/EMAIL-04, success criteria #2)
    - A forced failure (e.g. wrong app password) surfaces as a clear tool error, not a silent success (NOTIF-05/D-09)
  </acceptance_criteria>
  <done>The handshake echoes a supported protocol version and a real, correctly-formatted email is confirmed in the inbox (not spam).</done>
  <resume-signal>Type "approved" once the email lands in the inbox and the handshake echoes the version, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → /mcp | Untrusted HTTP clients call the tool over Streamable HTTP |
| tool args → service | Agent-supplied subject/body/status cross into rendering + send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing/Abuse | SendInput schema | mitigate | No recipient field on the tool input; recipient is config-only (SEC-02) |
| T-01-06 | Spoofing | StreamableHTTPHandler | mitigate | SDK localhost/DNS-rebinding protection is on for loopback; connect via http://localhost:8080/mcp (Pitfall 6). Bearer auth is Phase 2 |
| T-01-09 | Repudiation/Info | tool error path | mitigate | Failures surface verbatim as IsError tool errors (D-09); success returns message-id proof-of-delivery (D-08). Do not log secrets |
| T-01-04 | Spoofing | From alignment (live send) | mitigate | Verified at the gate: SPF/DMARC PASS via Show-original, From = GMAIL_USERNAME (EMAIL-04) |
</threat_model>

<verification>
- `go test ./...` green (handler success/error + renderer + channel unit tests)
- curl initialize to /mcp echoes a supported protocolVersion
- Manual deliverability gate: inbox-not-spam, SPF/DMARC PASS, [Status] subject, banner colors render
</verification>

<success_criteria>
The single `send_notification` tool is served over Streamable HTTP at /mcp with the correct schema and no recipient field; a successful call returns recipient+message-id+timestamp; a forced failure returns a clear tool error; and a real, correctly-formatted email lands in the inbox (not spam) from the authenticated account with the [Status] prefix.
</success_criteria>

<output>
Create `.planning/phases/01-mcp-tool-real-email-delivery/01-04-SUMMARY.md` when done
</output>
