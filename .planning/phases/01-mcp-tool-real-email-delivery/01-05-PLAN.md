---
phase: 01-mcp-tool-real-email-delivery
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - .vscode/launch.json
  - .devcontainer/devcontainer.json
autonomous: false
requirements: [DEVEX-01]
must_haves:
  truths:
    - "The server runs under the VS Code 'Debug mcp-notify' launch config inside the devcontainer with breakpoints hitting in cmd/server and handlers"
    - "A local MCP client connects to http://localhost:8080/mcp and successfully calls send_notification"
  artifacts:
    - path: ".vscode/launch.json"
      provides: "'Debug mcp-notify' launch config (verified, adjusted only if needed)"
      contains: "Debug mcp-notify"
  key_links:
    - from: ".vscode/launch.json"
      to: "cmd/server"
      via: "program points at the entrypoint with env mapped from .env"
      pattern: "cmd/server"
---

<objective>
Verify (not recreate) the local developer experience: the already-scaffolded devcontainer and the `.vscode/launch.json` "Debug mcp-notify" config let a developer run the server under the debugger with working breakpoints, and a local MCP client connects to `http://localhost:8080/mcp` and calls the tool end-to-end (DEVEX-01, success criterion #6).

Purpose: Confirms the debug loop now that real source files exist for breakpoints to bind to. Only touch the scaffolding if an actual defect blocks the debug session.
Output: A confirmed debug + connect workflow; minimal config edits only if required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md
@.vscode/launch.json
@.devcontainer/devcontainer.json
@.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify debug-in-devcontainer + local MCP client call</name>
  <files>.vscode/launch.json, .devcontainer/devcontainer.json (edit ONLY if a real defect blocks the session)</files>
  <what-built>The full Phase-1 server (from Plans 01-01..01-04) plus the pre-scaffolded devcontainer and "Debug mcp-notify" launch config.</what-built>
  <read_first>
    - .vscode/launch.json (the "Debug mcp-notify" config — mode debug, program cmd/server, env mapped from .env, dlvFlags --check-go-version=false)
    - .devcontainer/devcontainer.json (Go 1.25 feature, forwards 8080, installs air)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md §"Local Dev" (Inspector + curl handshake; A2 note on the Inspector package)
  </read_first>
  <action>
    Human-run verification gate. Reopen the repo in the devcontainer, run the "Debug mcp-notify" launch config, and confirm breakpoints bind to the real source and a local MCP client can call the tool. Follow how-to-verify exactly. Make NO code changes unless a real defect blocks the session — in which case make the minimal fix to `.vscode/launch.json` or `.devcontainer/devcontainer.json` and re-verify.
  </action>
  <how-to-verify>
    1. Open the repo in the devcontainer (Reopen in Container). Ensure `.env` exists with credentials filled (copied from `.env.example`).
    2. Run the "Debug mcp-notify" launch config. Set a breakpoint in `cmd/server/main.go` and in the `send_notification` handler (`internal/mcpserver/tool.go`).
    3. Connect a local MCP client to `http://localhost:8080/mcp`: either MCP Inspector (`npx @modelcontextprotocol/inspector`, transport = Streamable HTTP) or the curl handshake from RESEARCH §"Local Dev".
    4. Call `send_notification` and confirm the breakpoints in `cmd/server`/the handler are hit and the debugger steps through.
    5. Confirm the tool call completes (a notification is dispatched) end-to-end from the client.
  </how-to-verify>
  <verify>
    <human-check>Breakpoints in cmd/server and the handler are hit under "Debug mcp-notify"; a local MCP client at http://localhost:8080/mcp successfully calls send_notification.</human-check>
  </verify>
  <acceptance_criteria>
    - Breakpoints in `cmd/server` and the handler are hit under "Debug mcp-notify" (DEVEX-01)
    - A local MCP client connected to http://localhost:8080/mcp successfully calls send_notification (DEVEX-01, success criterion #6)
    - Any config edits are minimal and limited to fixing an actual blocker
  </acceptance_criteria>
  <done>The debug session hits breakpoints in real source and a local MCP client call to send_notification succeeds.</done>
  <resume-signal>Type "approved" once breakpoints hit and the local client call succeeds, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → devcontainer | Local dev tooling and the `.env` secrets file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | .env in devcontainer | mitigate | `.env` is gitignored and only mounted locally; never committed. Verify `.env` is not staged before any commit |
| T-01-06 | Spoofing | local /mcp endpoint | accept | Local loopback only; SDK localhost protection on. No public exposure until Phase 5 |
</threat_model>

<verification>
- "Debug mcp-notify" launches and breakpoints bind to real source
- Local MCP client (Inspector or curl) completes a send_notification call against http://localhost:8080/mcp
</verification>

<success_criteria>
A developer can debug the server in the devcontainer with working breakpoints and exercise `send_notification` from a local MCP client over http://localhost:8080/mcp — the Phase-1 local-dev success criterion is met.
</success_criteria>

<output>
Create `.planning/phases/01-mcp-tool-real-email-delivery/01-05-SUMMARY.md` when done
</output>
