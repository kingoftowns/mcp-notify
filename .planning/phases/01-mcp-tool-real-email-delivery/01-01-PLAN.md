---
phase: 01-mcp-tool-real-email-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/notify/service.go
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "`go build ./...` and `go test ./internal/config/...` succeed on a fresh checkout"
    - "Config loads PORT/GMAIL_USERNAME/GMAIL_APP_PASSWORD/BEARER_TOKEN/NOTIFY_RECIPIENT from env; recipient is server-side only"
    - "Renderer, Channel, and NotificationService seams exist as interfaces the rest of the phase implements against"
  artifacts:
    - path: "go.mod"
      provides: "Go module github.com/kingoftowns/mcp-notify with the five pinned deps"
      contains: "module github.com/kingoftowns/mcp-notify"
    - path: "internal/config/config.go"
      provides: "caarlos0/env struct + Load() over the .env.example contract"
      contains: "func Load"
    - path: "internal/notify/service.go"
      provides: "Renderer/Channel/NotificationService seams + Notify orchestration"
      contains: "type NotificationService"
  key_links:
    - from: "internal/notify/service.go"
      to: "Renderer + Channel"
      via: "NotificationService.Notify orchestrates Render then Send"
      pattern: "Notify"
    - from: "internal/config/config.go"
      to: ".env.example contract"
      via: "caarlos0/env struct tags"
      pattern: "env:\"NOTIFY_RECIPIENT\""
---

<objective>
Lay the structural spine of the walking skeleton: initialize the Go module with the exact pinned dependency set, load configuration from the fixed `.env.example` env contract, and define the three locked seam interfaces (`Renderer`, `Channel`, `NotificationService`) that every later slice implements against.

Phase user story (derived from ROADMAP Goal — not in user-story form; mvp-phase was not re-run): **As an** AI agent (neo or Claude Code), **I want to** call a single `send_notification` tool against a locally-run server, **so that** a correctly-formatted email reliably lands in Michael's inbox.

Purpose: Interface-first ordering — downstream plans (renderer, channel, wiring) receive the contracts in code instead of exploring for them. The recipient lives in config only (SEC-02), never as a tool parameter.
Output: A compiling module with config loading and the seam interfaces; nothing sends email yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md
@.planning/phases/01-mcp-tool-real-email-delivery/01-CONTEXT.md
@.env.example
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize module + pinned deps + config loader</name>
  <files>go.mod, go.sum, internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - .env.example (the fixed env-var contract: PORT, GMAIL_USERNAME, GMAIL_APP_PASSWORD, BEARER_TOKEN, NOTIFY_RECIPIENT)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md §"Standard Stack" (version pins) and §"Architecture Patterns → Recommended Project Structure"
    - CLAUDE.md (stack table + "What NOT to Use")
  </read_first>
  <behavior>
    - Load() with all required vars set returns a Config with matching field values
    - Load() with GMAIL_USERNAME / GMAIL_APP_PASSWORD / NOTIFY_RECIPIENT unset returns an error (required validators)
    - PORT defaults to 8080 when unset
  </behavior>
  <action>
    Run `go mod init github.com/kingoftowns/mcp-notify` (module path confirmed from the repo git remote — do not re-ask). Then `go get` the EXACT pinned versions: `github.com/modelcontextprotocol/go-sdk@v1.6.1`, `github.com/wneessen/go-mail@v0.7.3`, `github.com/yuin/goldmark@v1.8.2`, `github.com/microcosm-cc/bluemonday@v1.0.27`, `github.com/caarlos0/env/v11@v11.4.1`. These supersede the CLAUDE.md-stated go-sdk v1.7.0 / go-mail v0.6.2 (v1.7.0 stable does not exist; v0.7.3 is API-identical to v0.6.2). Set the `go` directive to 1.26 (toolchain floor 1.25). Run `go mod verify`.
    Create `internal/config/config.go`: a `Config` struct using `caarlos0/env/v11` tags mapping `PORT` (envDefault "8080"), `GMAIL_USERNAME` (required), `GMAIL_APP_PASSWORD` (required), `BEARER_TOKEN` (read into the struct for the contract but UNUSED this phase — bearer auth is Phase 2; do not wire it), `NOTIFY_RECIPIENT` (required). Expose `Load() (Config, error)` via `env.ParseAs`. Never log `GmailAppPassword` or `BearerToken`.
    Write `internal/config/config_test.go` covering the three behaviors above using `t.Setenv`.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/config/... && go mod verify</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/config/...` exits 0
    - `go mod verify` reports "all modules verified"
    - `grep -q 'module github.com/kingoftowns/mcp-notify' go.mod`
    - Config struct has no recipient-related tool parameter; NOTIFY_RECIPIENT is the only recipient source (SEC-02)
  </acceptance_criteria>
  <done>Module compiles; config loads the .env contract with required validation and an 8080 default.</done>
</task>

<task type="auto">
  <name>Task 2: Define the Renderer / Channel / NotificationService seams</name>
  <files>internal/notify/service.go</files>
  <read_first>
    - .planning/phases/01-mcp-tool-real-email-delivery/01-RESEARCH.md §"Architecture Patterns → Pattern 1: Seam interfaces" (the exact interface shapes)
    - .planning/phases/01-mcp-tool-real-email-delivery/01-CONTEXT.md (D-08 success result needs a message-id from Channel)
    - internal/config/config.go (Config type from Task 1)
  </read_first>
  <action>
    Create `internal/notify/service.go` defining the three locked seams exactly as RESEARCH Pattern 1 specifies: a `Rendered` struct with `Subject`, `HTML`, `Text` string fields; a `Renderer` interface with `Render(subject, body, status string) (Rendered, error)`; a `Channel` interface with `Send(ctx context.Context, r Rendered) (messageID string, err error)` (the returned message-id is what makes D-08 proof-of-delivery possible without leaking go-mail types upward); and a `NotificationService` struct holding `R Renderer`, `C Channel`, `Recipient string`. Add the orchestration method `Notify(ctx, subject, body, status string) (messageID string, ts time.Time, err error)` that calls `R.Render` then `C.Send`, returning the message-id and a `time.Now()` delivery timestamp on success, or the propagated error verbatim (D-09 — no retry hint, no transient/permanent classification). Do NOT implement Renderer or Channel here — those are separate plans. Keep the seams clean: no bearer auth, no SecretProvider, no Slack (all later phases).
  </action>
  <verify>
    <automated>go build ./internal/notify/...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/notify/...` exits 0
    - service.go declares `Renderer`, `Channel`, `NotificationService`, `Rendered`, and `Notify` (source assertion)
    - `Channel.Send` returns `(messageID string, err error)` so D-08 can report the id
    - No recipient parameter appears in any interface method (recipient is the `NotificationService.Recipient` config field — SEC-02)
  </acceptance_criteria>
  <done>The three seams compile and Notify orchestrates Render→Send; renderer/channel impls slot in next wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | Secrets (Gmail app password, bearer token) enter via env vars |
| go module proxy → build | Third-party dependency code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | config.go secret fields | mitigate | Never log `GmailAppPassword`/`BearerToken`; no `String()`/`%+v` dump of Config that includes them (structured-log redaction is Phase 2) |
| T-01-03 | Spoofing/Abuse | recipient sourcing | mitigate | Recipient sourced only from `NOTIFY_RECIPIENT` config; no recipient field on any seam (SEC-02) |
| T-01-SC | Tampering | go module installs | mitigate | All five modules verified against proxy.golang.org in RESEARCH Package Legitimacy Audit (all Approved; no SLOP/SUS/ASSUMED). Gate with `go mod verify` + `govulncheck ./...` — no blocking human checkpoint required |
</threat_model>

<verification>
- `go build ./...` succeeds
- `go test ./internal/config/...` green
- `go mod verify` passes; `go.mod` pins go-sdk v1.6.1 and go-mail v0.7.3 (the corrected versions)
</verification>

<success_criteria>
Module initialized with the exact pinned stack; config loads the `.env.example` contract with required validation; the `Renderer`/`Channel`/`NotificationService` seams compile and expose `Notify`. Recipient is config-only (SEC-02).
</success_criteria>

<output>
Create `.planning/phases/01-mcp-tool-real-email-delivery/01-01-SUMMARY.md` when done
</output>
